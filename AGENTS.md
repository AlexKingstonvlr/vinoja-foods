# AGENTS.md — Vinoja Foods Project

> **Project Status: Empty Directory**  
> This directory (`/Users/apple/Documents/Antigravity/Vinoja Foods`) is currently **empty** — no source code, configuration files, or project structure exists yet.

---

## Project Overview

**Project Name:** Vinoja Foods  
**Location:** `/Users/apple/Documents/Antigravity/Vinoja Foods`  
**Status:** Empty directory — no existing codebase, configuration, or documentation found.

This directory is empty and ready for a new project to be initialized.

---

## Technology Stack

**Not yet determined.** The technology stack, language, framework, and tooling have not been chosen yet.

---

## Project Structure

```
Vinoja Foods/
└── (empty)
```

No source directories, configuration files, tests, or documentation exist yet.

---

## Build & Test Commands

**Not applicable** — no build system, test runner, or package manager configured.

---

## Code Style & Conventions

**Not yet established.** When initializing the project, consider adopting conventions for:

- Language-specific style guides (e.g., PEP 8 for Python, StandardJS for JS/TS, rustfmt for Rust)
- Linting/formatting tools (ruff/black, eslint/prettier, ruff/clippy, etc.)
- Commit message conventions (Conventional Commits, etc.)
- Branching strategy (GitFlow, trunk-based, etc.)

---

## Testing Strategy

**Not yet defined.** When initializing the project, consider:

- Unit test framework (pytest, vitest, cargo test, etc.)
- Integration test strategy
- CI/CD pipeline configuration
- Coverage targets

---

## Development Workflow

**Not yet established.** Recommended setup for a new project:

1. Initialize version control (`git init`)
2. Choose language/framework and initialize project (e.g., `npm init`, `cargo new`, `poetry new`, `go mod init`)
2. Add linting/formatting configuration
3. Set up test framework and CI
4. Create initial `README.md` and `AGENTS.md` with project-specific guidance

---

## Security Considerations

**Not applicable yet.** When the project is initialized, consider:

- Dependency scanning (Dependabot, cargo-audit, npm audit, etc.)
- Secret scanning (git-secrets, truffleHog)
- License compliance
- Supply chain security (Sigstore, SLSA)

---

## Deployment

**Not applicable.** No deployment target or infrastructure defined.

---

## Next Steps for Initialization

When initializing this project, consider:

1. **Choose a technology stack** — e.g.:
   - Python: `poetry new` / `uv init` / `uv init --package`
   - Node.js/TypeScript: `npm create` / `pnpm create` / `bun create`
   - Rust: `cargo new`
   - Go: `go mod init`
   - Java/Kotlin: Gradle/Maven init

2. **Add configuration files:**
   - `pyproject.toml` / `package.json` / `Cargo.toml` / `go.mod` / `build.gradle.kts`
   - Lint/format config (`.prettierrc`, `ruff.toml`, `.eslintrc`, `rustfmt.toml`)
   - Git ignore (`.gitignore`)
   - Editor config (`.editorconfig`)

3. **Set up testing** — add test framework, example test, CI config

4. **Update this AGENTS.md** with project-specific details after initialization

---

## Notes for Future AI Agents

> **This project is empty.** There is no existing codebase to understand, no conventions to follow, and no tests to run. The first task will be **project initialization**. When that happens, **replace this entire file** with a project-specific AGENTS.md documenting the actual stack, structure, conventions, and workflows.